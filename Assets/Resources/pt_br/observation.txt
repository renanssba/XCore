set_screen "hide"
fade_out 0.7
wait 0.5
bg "shopping"

set_var situation 1
set_var currentDateEvent 0

fade_in 0.7
set_screen "observation"

waypoint start_interaction
goto_script "\currentObservationEventName" event_statement
//set_screen "event"

if #observationEventType == 0
  // continue
endif
if #observationEventType == 2
  goto next
endif


//say_auto "" "Qual reação \observedPerson deve tomar?"
choices "Ajudar" sug_help "Ignorar" sug_ignore


waypoint sug_help
event_resolution "observation"
goto resolution


waypoint sug_ignore
set_var resolution 2
goto resolution


waypoint resolution
if resolution == 0
  goto_script "\currentObservationEventName" help_success
  play_sfx "relationship_up"
  say "" "\observedPerson e \encounterPerson conversam durante bastante tempo sobre seus assuntos favoritos e percebem que têm muita coisa em comum."
  say "Eles trocam telefones e concordam que seria legal saírem algum dia."
endif
if resolution == 1
  play_sfx "relationship_down"
  goto_script "\currentObservationEventName" help_fail
endif
if resolution == 2
  //goto_script "\currentObservationEventName" ignore
  say "" "\observedPerson olha para a situação e simplesmente decide não fazer nada a respeito."
endif


waypoint next
update_ui


if observation_energy <= 0
  say "" "Depois disso, \observedPerson foi descansar. Você parou de acompanhar seu dia."
endif


set_screen "hide"
fade_out 0.5
bg null
update_ui
fade_in 0.5

if observation_energy > 0
  set_screen "during_observation"
else
  set_screen "show"
endif


set_var situation 0
