fade_out 1
pass_time
bg null
goto_script "cap1_interaction_pins"
goto_script "show_people_screen"
update_ui
//fade_in 1

if daytime == 0
  goto_script "cap1_manha"
endif
if daytime == 1
  goto_script "cap1_pos_aula"
endif
if daytime == 2
  goto_script "cap1_pos_tarde"
endif
