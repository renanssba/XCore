if #currentRelationshipHeartLocksOpened < 3
  fade_music 0 1
  fade_out 1
endif

goto_script "check_end_game"
if game_has_ended == 1
  end_script
endif

if daytime == 2
  goto_script "cap1_late_night"
  /// CHECK TO SEE IF ACHIEVEMENT SLEEP ALL DAY
  if times_slept_today >= 3
    get_achievement "SLEEP_ALL_DAY"
  endif
  set_var times_slept_today 0
endif

pass_time
save 0
goto_script "select_daytime_interaction"

