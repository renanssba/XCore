if tutorial_date2 == 1
  goto end
endif

say "Fertiliel" "Certos desafios devem ser solucionados pelo garoto, outros pela garota, vai depender da situação."
if #currentEventInteractionType == 0
  say "Este é um desafio que o garoto deve resolver. Preste atenção nos atributos dele:"
endif
if #currentEventInteractionType == 1
  say "Este é um desafio que a garota deve resolver. Preste atenção nos atributos dela:"
endif

//wait 0.5

if #currentPersonality == 0
  set_var attr "<color=yellow><sprite='Attributes' index=0 tint>Valentia(a)</color>"
endif
if #currentPersonality == 1
  set_var attr "<color=#248BCF><sprite='Attributes' index=1 tint>Inteligência</color>"
endif
if #currentPersonality == 2
  set_var attr "<color=red><sprite='Attributes' index=2 tint>Carisma</color>"
endif
say "Há uma estrelinha do lado do atributo \vsn[attr]. Isso quer dizer que ele é o atributo preferido da pessoa, e que ela sempre tentará utilizá-lo sua para resolver as situações."

say "Você pode deixar a pessoa utilizar seu atributo preferido ou sugestioná-la para utilizar outro atributo na situação, mas isso consome seu <color=yellow>AP</color>."

say "Os desafios podem ser resolvidos com <color=yellow>qualquer um dos atributos</color>, mas a <color=yellow>eficácia do atributo</color> utilizado varia de desafio para desafio."
say "Portanto, ajude seus humanos a tomar sempre a atitude que pareça mais certa para a situação!"


goto_script "fertiliel_exit"

waypoint end
set_var tutorial_date2 1
update_ui
